r"""campaign_r60_information_frontier - the INFORMATION FRONTIER offensive.

RESEARCH ONLY. PAPER ONLY. NO ORDERS, NO FILLS, NO PROMOTION, NO ADOPTION.
NEW_PAID_DATA_COST = $0.

The estate has settled 8,445 hypotheses with 0 qualified survivors. 97.4% of
them used PRICE_STATE information only. This campaign does not add a 8,446th
transformation of the same information; it spends its budget on information the
estate has never read.

A package, not a loose module, so ``panels`` and ``executors`` import each
other normally and the substrate is loaded ONCE per process (the runner's
``load_executors`` imports a campaign laid out this way as a package for
exactly that reason).
"""
from __future__ import annotations

CAMPAIGN_ID = "R60_INFORMATION_FRONTIER"
RUN_ID = "INFORMATION_FRONTIER_ALPHA_OFFENSIVE_V1"
