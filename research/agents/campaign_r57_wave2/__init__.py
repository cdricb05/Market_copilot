"""campaign_r57_wave2 - the research content of ALPHA WAVE 2 (R57).

RESEARCH ONLY. PAPER ONLY. NO ORDERS, NO FILLS, NO PROMOTION.

A campaign's hypotheses are its own content and live with the campaign; the
execution path (`alpha_agent.agents_v2.runner`), the books
(`alpha_agent.agents_v2.books`), the gate (`alpha_agent.r59.engines.gate`) and
the registry (`alpha_agent.r59.memory`) are canonical owners this package only
calls.

    panels.py     the certified substrates and the point-in-time universes
    executors.py  the ten hypotheses, one function each, plus EXECUTORS
"""
CAMPAIGN_ID = "R57_ALPHA_WAVE2"
